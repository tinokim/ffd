<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ffd.mapper.UserMapper">

    <insert id="insertUser" parameterType="com.ffd.model.User">
        INSERT INTO users (id, pw, name, address, phone, role, email, user_insert_date, user_update_date)
        VALUES (#{id}, #{pw}, #{name}, #{address}, #{phone}, #{role}, #{email}, #{userInsertDate}, #{userUpdateDate})
    </insert>

    <select id="findByEmail" resultType="com.ffd.model.User">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <update id="updateUserPassword">
        UPDATE users SET pw = #{newPassword} WHERE user_seq = #{userSeq}
    </update>

</mapper>
