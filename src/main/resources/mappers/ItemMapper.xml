<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ffd.api.mapper.ItemMapper">
    <!-- 아이템 목록 조회 -->
    <select id="findList" resultType="com.ffd.model.Item">
        SELECT item_seq, item_name, item_price, item_stock, item_status, insert_date, update_date, item_detail
        FROM items
    </select>

    <!-- 특정 아이템 상세 정보 조회 -->
    <select id="findDetail" parameterType="long" resultType="com.ffd.model.Item">
        SELECT item_seq, item_name, item_price, item_stock, item_status, insert_date, update_date, item_detail
        FROM items
        WHERE item_seq = #{item_seq}
    </select>
</mapper>
